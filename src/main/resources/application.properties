spring.application.name=ApiApexManagent

# Configuración de la base de datos
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT config (debe coincidir con .NET)
jwt.secret=esta-es-una-clave-super-secreta-para-proteger-tus-tokens-jwt
jwt.issuer=https://localhost:7201
jwt.audience=AppCliente

personal.api.url=https://gateway-api-vweu.onrender.com/ApiSeguridad/api/personal
